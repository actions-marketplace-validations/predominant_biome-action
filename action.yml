name: 'Biome Install'
author: 'Graham Weldon'
description: 'Install Biome for pipeline usage'
inputs:
  version:
    description: 'version to install, default is latest'
    required: false
runs:
  using: 'node14'
  main: 'index.js'
branding:
  icon: 'bold'
  color: 'green'
